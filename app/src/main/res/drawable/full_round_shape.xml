<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/topBottomColor"/> <!-- fill color -->
    <size android:width="70dp" android:height="70dp"/> <!-- oval size -->
    <stroke android:color="@color/onBackground" android:width="2dp"/>
</shape>
